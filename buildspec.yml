
version: 0.2
    
phases:
  install:
    run-as: root
    runtime-versions:
      nodejs: 12
    commands:
      - npm install
  build:
    run-as: root
    commands:
      - npm run release
artifacts:
  files:
    - build/*
    - build/**/*